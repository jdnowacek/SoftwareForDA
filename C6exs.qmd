---
title: "C6exs"
format: pdf
editor: visual
---

```{r}
library(tidyverse)
```

\

```{r}
x <- matrix(1:12, 3, 4)
x
```

```{r}
fmax <- function(x){
  return(max(x))
}

y <- c(1, 2, 3, 4, 5)

fmax(y)
```

```{r}
maxes <- apply(x, 2, fmax)
maxes
```

Solution:

```{r}
t(apply(x, 2, range))
```
